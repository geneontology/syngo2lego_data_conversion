language: scala

# TODO: wrap details in shell or makefile to => iteration of multiple json files.

script
 - git clone https://github.com/geneontology/syngo2lego.git
 - cd syngo2lego
 - sbt "run -dev ../json/SynGO.json"
 - mv SYNGO* ../ttl/.
 - cd ..
 - git add ttl/*
 - git commit -m'Travis autocommit'
